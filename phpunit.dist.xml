<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true">
    <php>
        <env name="KERNEL_CLASS" value="Uhifadhi\Roster\Tests\Integration\TestKernel"/>
        <!--
            No database yet: the module has no entities until the roster design
            rules on the domain model, so the integration suite only boots the
            kernel. This URL is never connected to — it exists because
            DoctrineBundle requires a DBAL url to compile, and it is the shape
            the real one will take (the fundi cluster, port 5434).
        -->
        <env name="ROSTER_TEST_DATABASE_URL"
             value="postgresql://app:app@127.0.0.1:5434/roster_bundle_test?serverVersion=17&amp;charset=utf8"/>
    </php>
    <testsuites>
        <testsuite name="default">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <source restrictNotices="true" restrictWarnings="true">
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
